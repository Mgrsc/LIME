<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="20dp"
    android:paddingBottom="12dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/handwriting_paint_thickness"
        android:textColor="@color/settings_primary_text"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Stroke preview area -->
    <org.bitfennec.lime.view.widget.HandwritingStrokePreviewView
        android:id="@+id/stroke_preview_view"
        android:layout_width="match_parent"
        android:layout_height="96dp"
        android:layout_marginTop="16dp" />

    <!-- Value indicator and slider -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/handwriting_thickness_title"
            android:textColor="@color/settings_primary_text"
            android:textSize="14.5sp" />

        <TextView
            android:id="@+id/tv_dialog_thickness_val"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/handwriting_thickness_default"
            android:textColor="@color/settings_accent_color"
            android:textSize="14.5sp"
            android:textStyle="bold" />
    </LinearLayout>

    <SeekBar
        android:id="@+id/seekbar_thickness"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:max="100"
        android:progress="35" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="@string/handwriting_preview_hint"
        android:textColor="@color/settings_secondary_text"
        android:textSize="12sp" />

</LinearLayout>
